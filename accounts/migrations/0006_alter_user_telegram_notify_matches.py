# Generated by Django 5.2.15 on 2026-06-15 21:14

from django.db import migrations, models


def enable_for_linked_members(apps, schema_editor):
    """Switch the new match-event DMs on for everyone already linked to the bot.

    The field shipped as opt-out-by-default's opposite (default False in 0005),
    so existing rows are all False; flipping it to match the new default=True
    means current members get match events without having to re-toggle. Only
    linked accounts (a chat id) can actually receive a DM, so scope to those.
    """
    User = apps.get_model("accounts", "User")
    User.objects.filter(telegram_chat_id__isnull=False).update(
        telegram_notify_matches=True,
    )


class Migration(migrations.Migration):

    dependencies = [
        ('accounts', '0005_user_telegram_notify_matches'),
    ]

    operations = [
        migrations.AlterField(
            model_name='user',
            name='telegram_notify_matches',
            field=models.BooleanField(default=True, verbose_name='اعلان رویدادهای بازی فعال'),
        ),
        migrations.RunPython(
            enable_for_linked_members, migrations.RunPython.noop,
        ),
    ]
